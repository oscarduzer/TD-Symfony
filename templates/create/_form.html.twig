
        {{ form_start(form) }}
                <div class="p-2 w-100">
                    <label for="nomMarche" class="form-label">Nom du marché *</label>
                    {{ form_widget(form.nomMarche) }}
                </div>
                <div class="p-2 w-100">
                    <label for="description" class="form-label">Description *</label>
                    {{ form_widget(form.description) }}
                </div>
                <div class="p-2 w-100">
                    <label for="capacite" class="form-label"> Capacité du marché *</label>
                    {{ form_widget(form.capacite) }}
                </div>
                <div class="p-2 w-100">
                    <label for="adresse" class="form-label"> Adresse *</label>
                    {{ form_widget(form.adresse) }}
                </div>
                <div class="p-2 w-100">
                    <label for="tel" class="form-label"> Téléphone *</label>
                    {{ form_widget(form.telephone) }}
                </div>
                <div class="p-2 w-100">
                    <label for="image" class="form-label"> Image *</label>
                    {{ form_widget(form.imageFile) }}
                </div>
                <div class="p-2 w-100">
                    <label for="ville" class="form-label"> Ville *</label>
                    {{ form_widget(form.idVille) }}
                </div>
                <div class="d-flex justify-content-between p-2 w-100">
                    <a class="btn btn-danger" href="{{ path('app_create_index') }}">Annuler</a>
                    <button type="submit" class="btn btn-success">Enregistrer</button>
                </div>
        {{ form_end(form) }}